# Database Configuration
DATABASE_URL=postgresql://neondb_owner:npg_8QatAVFqB6Cf@ep-icy-star-a7lngkfl-pooler.ap-southeast-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require


# Example for Neon Serverless PostgreSQL:
# DATABASE_URL=postgresql://user:password@ep-cool-name-123456.us-east-2.aws.neon.tech/neondb?sslmode=require

# JWT Authentication Secret
# CRITICAL SECURITY REQUIREMENT:
# 1. Generate a strong secret using: openssl rand -base64 32
# 2. This secret MUST match AUTH_SECRET in frontend/.env.local
# 3. Minimum 32 characters required for HS256 security
# 4. NEVER commit the actual secret to version control
# 5. Use different secrets for development and production
#
# Example generation:
#   $ openssl rand -base64 32
#   Output: xK8vN2mP9qR4sT6uW7yZ0aB1cD3eF5gH8iJ9kL0mN2oP4qR6sT8u
#
# Then copy the output to both:
#   - Backend: backend/.env as JWT_SECRET=<generated-secret>
#   - Frontend: frontend/.env.local as AUTH_SECRET=<generated-secret>
JWT_SECRET=zF9nkn/yW1wAv6JznI0n3TgZ8fIngRW9sSSpBdBoDJo=

# CORS Configuration (Optional)
# Comma-separated list of allowed origins for CORS
# Development default: http://localhost:3000
# Production: Specify your frontend domain(s)
# Example: CORS_ORIGINS=https://myapp.com,https://www.myapp.com
# CORS_ORIGINS=http://localhost:3000

# Environment (Optional)
# Set to "production" to enable HTTPS-only security headers
# ENVIRONMENT=development
