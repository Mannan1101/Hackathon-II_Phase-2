# Authentication Secret
# CRITICAL SECURITY REQUIREMENT:
# 1. Generate a strong secret using: openssl rand -base64 32
# 2. This secret MUST match JWT_SECRET in backend/.env
# 3. Minimum 32 characters required for HS256 security
# 4. NEVER commit the actual secret to version control
# 5. Use different secrets for development and production
#
# Example generation:
#   $ openssl rand -base64 32
#   Output: xK8vN2mP9qR4sT6uW7yZ0aB1cD3eF5gH8iJ9kL0mN2oP4qR6sT8u
#
# Then copy the output to both:
#   - Frontend: frontend/.env.local as AUTH_SECRET=<generated-secret>
#   - Backend: backend/.env as JWT_SECRET=<generated-secret>
AUTH_SECRET=your-super-secret-key-min-32-characters-long-for-hs256-security

# Backend API URL
# Development: http://localhost:8000
# Production: Your deployed backend URL (must be HTTPS in production)
# Example: NEXT_PUBLIC_API_URL=https://api.myapp.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# Database URL (for Better Auth)
# Better Auth requires database connection for user storage
# Use the same DATABASE_URL as your backend
# Example for Neon: postgresql://user:password@ep-name.region.aws.neon.tech/neondb?sslmode=require
# DATABASE_URL=postgresql://user:password@host:port/database

